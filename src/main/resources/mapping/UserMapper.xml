<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.demo.entity.User">
        <id column="userId" property="userId" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="selectUserList" resultType="com.example.demo.entity.User">
        select * from a_user
    </select>

    <select id="getUserName" resultType="String">
        select username from a_user where username = #{username}
    </select>

    <select id="getPassWord" resultType="String">
        select password from a_user where password = #{password}
    </select>

    <insert id="userRegister" parameterType="com.example.demo.entity.User">
        insert into a_user(username,password) values (#{username,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR})
    </insert>
</mapper>